# so-django-demos

Used to demo certain concepts based on questions on [*StackOverflow*](https://stackoverflow.com).

By default it works with an sqlite database with a small amount of sample data.

The database also has a user `test` with `test` as password to log in.